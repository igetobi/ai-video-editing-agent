# Python deps for the pipeline.
#
# The pipeline scripts use ONLY the standard library — the core runs on a bare
# Python 3.10+. Transcription is external:
#
#   * Default (light): whisper.cpp via HyperFrames — no Python package needed.
#       macOS: brew install whisper-cpp   (see docs/SETUP.md / docs/DOCKER.md)
#   * Optional (heavy): WhisperX — pulls in PyTorch. Install only if you want it:
#       pipx install whisperx
#
# So there are no required pip installs here. The line below is OPTIONAL.

# whisperx>=3.1.1      # uncomment only if you use `transcribe.py --engine whisperx`

# Dev / test only:
pytest>=7.0
