# Music

Drop background tracks here, or reference any path when mixing:

```
python scripts/background_music.py --job "<job>" --music assets/music/bed.mp3 --gain -23
```

A copy of whatever track you use is saved into the job's `music/` folder for
reproducibility. Use tracks you have the rights to.
