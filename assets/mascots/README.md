# Mascots / characters

Optional. Drop a character PNG here (e.g. `mascot.png`) and reference it via
`config/brand.tokens.json → mascot.image`. Some beats can bounce/glow it in as an
accent (the demo used a bouncing, glowing mascot on its first card).

Transparent PNG recommended.
