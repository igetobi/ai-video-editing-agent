# Logos

Put your logo files here and reference them from `config/brand.tokens.json`:

- `logo-mark.png` — square/icon mark (used for the corner logo bug).
- `logo-wordmark.png` — horizontal wordmark.

Transparent PNGs (or SVGs) look best over footage. Corner and opacity are set in
`brand.tokens.json → logo`.
